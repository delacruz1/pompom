import React from "react";
import PropTypes from "prop-types";
import "./Task.css";
import Typography from "@mui/material/Typography";


//Todo: Task logic and task section for app.

const Task = (taskId, taskDetails) => {
  return (
  <div className="Task">

  </div>
  );
};

export default Task;
